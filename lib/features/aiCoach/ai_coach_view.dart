import 'package:flutter/material.dart';

class AiCoachView extends StatelessWidget {
  const AiCoachView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('AiCoach')));
  }
}
